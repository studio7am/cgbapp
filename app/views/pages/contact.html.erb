<div id="page">

<div class="left">
	<h2>Контакты</h2>
</div>

<div class="right">
    <% @pagecontactimagesone.each do |pageimage| %>
        <img src="<%= pageimage.pgimg %>" class="img-responsive">
    <% end %>
    <% @pagecontactimagestwo.each do |pageimage| %>
        <img src="<%= pageimage.pgimg %>" class="img-responsive">
    <% end %>
</div>

<div class="pageContent">
<div class="contentLeft">
<div class="contactBox">
    <% @contactstext.each do |abouttext| %>
        <h3><%= abouttext.title %></h3>
        <%= raw abouttext.content %>

    <% end %>


</div>
</div>

<div class="contentRight">
<%=render 'partials/homeAside' %>

  <hr>
</div>
</div>




</div>