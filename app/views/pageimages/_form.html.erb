<%= form_for(@pageimage) do |f| %>
  <% if @pageimage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pageimage.errors.count, "error") %> prohibited this pageimage from being saved:</h2>

      <ul>
      <% @pageimage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.file_field :pgimg %>
    <%= f.hidden_field :pgimg_cache %>

  </div>
  <div class="actions">
    <%= f.submit  'Загрузить', class: 'btn btn-success' %>
  </div>
<% end %>
