
<table class="table">
  <thead>
    <tr>
      <th>Загруженый документ</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @jobs.each do |job| %>
      <tr>
        <td><%= job.dwnld %></td>
        <td><%= link_to 'Загрузить другой документ', edit_job_path(job) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

